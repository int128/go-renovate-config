name: validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    uses: int128/renovate-config-validator-workflow/.github/workflows/validate.yaml@6670731131435c4a913dec6899d740c3953f3a95 # main
    with:
      config: .github/renovate.* *.json
