name: validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    uses: int128/renovate-config-validator-workflow/.github/workflows/validate.yaml@a788d1f5625ae543a008a07f43581b0a5d934300 # main
    with:
      config: .github/renovate.* *.json
