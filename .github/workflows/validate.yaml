name: validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    uses: int128/renovate-config-validator-workflow/.github/workflows/validate.yaml@b267869e652f7137b59e752d41121af4e177e791 # main
    with:
      config: .github/renovate.* *.json
