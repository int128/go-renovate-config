name: validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    uses: int128/renovate-config-validator-workflow/.github/workflows/validate.yaml@72722412f36b61cc389e77f15127028f15994848 # main
    with:
      config: .github/renovate.* *.json
