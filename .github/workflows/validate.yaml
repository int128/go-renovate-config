name: validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    uses: int128/renovate-config-validator-workflow/.github/workflows/validate.yaml@a6ecad4d09dc40293c0e22c9d1946e313f53814f # main
    with:
      config: .github/renovate.* *.json
