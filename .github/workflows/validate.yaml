name: validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    uses: int128/renovate-config-validator-workflow/.github/workflows/validate.yaml@60fdae1bff6c8a96fa685d7173154e370c46598d # main
    with:
      config: .github/renovate.* *.json
